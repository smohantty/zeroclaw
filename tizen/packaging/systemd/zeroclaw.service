[Unit]
Description=ZeroClaw daemon
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/.zeroclaw/workspace
ExecStart=/usr/bin/zeroclaw --config-dir /root/.zeroclaw daemon
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
